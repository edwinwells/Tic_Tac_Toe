<center><h1>Enter a square:</h1><center>

<form method="post" action="ComputerO_balance">
  <input type="text" name = "move">
  <input type="submit">
</form>

	<h1><center><table>
	  <tr>
	    <td><%= gameboard.setup[:a3]%></td>
	    <td><%= gameboard.setup[:b3]%></td>
	    <td><%= gameboard.setup[:c3]%></td>
	  </tr>
	  <tr>
	    <td><%= gameboard.setup[:a2]%></td>
	    <td><%= gameboard.setup[:b2]%></td>
	    <td><%= gameboard.setup[:c2]%></td>
	  </tr>
	  <tr>
	    <td><%= gameboard.setup[:a1]%></td>
	    <td><%= gameboard.setup[:b1]%></td>
	    <td><%= gameboard.setup[:c1]%></td>
	  </tr>
	</table></center></h1>
