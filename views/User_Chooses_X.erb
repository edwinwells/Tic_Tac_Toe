<center><h1>Enter a square:</h1><center>

<form method="post" action="humanX(move)">
  <input type="text" name = "move">
  <input type="submit">
</form>

<table>
  <tr>
    <td><%= newboard.setup[:a1]%></td>
    <td><%= newboard.setup[:a2]%></td>
    <td><%= newboard.setup[:a3]%></td>
  </tr>
  <tr>
    <td><%= newboard.setup[:b1]%></td>
    <td><%= newboard.setup[:b2]%></td>
    <td><%= newboard.setup[:b3]%></td>
  </tr>
  <tr>
    <td><%= newboard.setup[:c1]%></td>
    <td><%= newboard.setup[:c2]%></td>
    <td><%= newboard.setup[:c3]%></td>
  </tr>
</table>